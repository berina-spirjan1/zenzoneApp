import { createTheme } from 'react-native-theming'

export const themes = [
    createTheme({
        backgroundColor: '#CBDBF2',
        color: '#000000',
    }, 'light'),
    createTheme({
        backgroundColor: '#646668',
        color: '#FFFFFF'
    })
]
